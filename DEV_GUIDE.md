# Руководство по разработке с Hot Reload

## 🚀 Быстрый старт для разработки

### Запуск в режиме разработки (с hot reload)

```bash
# Запустить все сервисы с hot reload
make dev

# Или напрямую через docker-compose
docker-compose -f docker-compose.dev.yml up -d
```

### Остановка

```bash
make dev-stop

# Или
docker-compose -f docker-compose.dev.yml down
```

## 🔥 Hot Reload

В режиме разработки изменения применяются автоматически **без пересборки контейнеров**:

### Backend (Python/FastAPI)

- ✅ Изменения в `app/` автоматически перезагружают API
- ✅ Uvicorn запущен с флагом `--reload`
- ✅ Изменения видны через 1-2 секунды

### Frontend (Next.js/TypeScript)

- ✅ Изменения в `frontend/` автоматически перезагружают страницу
- ✅ Next.js встроенный hot reload
- ✅ Изменения видны мгновенно

### Worker (Celery)

- ⚠️ Изменения в `app/tasks/` применяются после выполнения следующей задачи
- ⚠️ Celery использует `--max-tasks-per-child=1` для перезагрузки после каждой задачи
- 💡 Для немедленного применения изменений: `docker-compose -f docker-compose.dev.yml restart worker`

## 📁 Структура файлов

```
quanttxt/
├── app/                    # Python код (монтируется в контейнер)
├── frontend/               # Next.js код (монтируется в контейнер)
├── docker-compose.yml       # Production конфигурация
└── docker-compose.dev.yml   # Development конфигурация (с hot reload)
```

## 🔄 Разница между dev и prod

### Development (`docker-compose.dev.yml`)

- ✅ **Hot reload** включен
- ✅ Код монтируется как volume
- ✅ Более подробные логи
- ✅ DEBUG режим включен
- ⚠️ Медленнее из-за volume mounting

### Production (`docker-compose.yml`)

- ❌ Hot reload отключен
- ✅ Код копируется в образ при сборке
- ✅ Оптимизированная производительность
- ✅ Минимальные логи

## 🛠️ Работа с изменениями

### Изменение Python кода

1. Отредактируйте файл в `app/`
2. Сохраните файл
3. Uvicorn автоматически перезагрузит API (через 1-2 сек)
4. Проверьте логи: `docker logs quanttxt-api-dev -f`

### Изменение TypeScript/React кода

1. Отредактируйте файл в `frontend/`
2. Сохраните файл
3. Next.js автоматически перезагрузит страницу
4. Изменения видны в браузере мгновенно

### Изменение Celery задач

1. Отредактируйте файл в `app/tasks/`
2. Сохраните файл
3. Celery worker автоматически перезагрузится (через 2-3 сек)
4. Проверьте логи: `docker logs quanttxt-worker-dev -f`

## 📊 Просмотр логов

```bash
# Все сервисы
make docker-dev-logs

# Конкретный сервис
docker logs quanttxt-api-dev -f      # API
docker logs quanttxt-worker-dev -f    # Worker
docker logs quanttxt-frontend-dev -f  # Frontend
```

## 🔧 Устранение проблем

### Изменения не применяются

1. **Проверьте, что используете dev конфигурацию:**

   ```bash
   docker-compose -f docker-compose.dev.yml ps
   ```

2. **Проверьте, что volumes смонтированы:**

   ```bash
   docker inspect quanttxt-api-dev | grep -A 10 Mounts
   ```

3. **Перезапустите сервис:**
   ```bash
   docker-compose -f docker-compose.dev.yml restart api
   ```

### Ошибки импорта в Python

Если добавили новый файл/модуль:

```bash
# Перезапустите API
docker-compose -f docker-compose.dev.yml restart api
```

### Frontend не обновляется

1. Проверьте, что Next.js запущен в dev режиме:

   ```bash
   docker logs quanttxt-frontend-dev | grep "ready"
   ```

2. Очистите кеш Next.js:
   ```bash
   docker exec quanttxt-frontend-dev rm -rf .next
   docker-compose -f docker-compose.dev.yml restart frontend
   ```

## 🎯 Рекомендации

1. **Используйте dev режим для разработки** - не нужно пересобирать контейнеры
2. **Используйте prod режим для тестирования** - проверяйте финальную сборку
3. **Следите за логами** - они покажут ошибки и перезагрузки
4. **Не коммитьте `docker-compose.dev.yml` изменения** в production

## 📝 Примеры команд

```bash
# Запуск dev режима
make dev

# Просмотр логов API
docker logs quanttxt-api-dev -f

# Перезапуск только API
docker-compose -f docker-compose.dev.yml restart api

# Остановка dev режима
make dev-stop

# Запуск production режима
make prod

# Остановка production режима
make prod-stop
```
