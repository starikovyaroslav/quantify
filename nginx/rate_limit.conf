# Additional rate limiting configuration
# This file is included in nginx.conf

# Limit connections per IP
limit_conn_zone $binary_remote_addr zone=perip:10m rate=10r/s;

# Additional protection against slow requests
client_body_timeout 10s;
client_header_timeout 10s;




