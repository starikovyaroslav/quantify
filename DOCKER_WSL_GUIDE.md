# Docker Desktop и WSL2 - Руководство

## 🤔 Нужен ли WSL2 для Docker Desktop?

### Короткий ответ: **НЕ обязателен, но РЕКОМЕНДУЕТСЯ**

Docker Desktop на Windows может работать в двух режимах:

1. **С WSL2** (рекомендуется) ✅
2. **Без WSL2** (через Hyper-V) ⚠️

## 📊 Сравнение режимов

### С WSL2 (рекомендуется)

**Преимущества:**
- ✅ Быстрее работает (до 20x быстрее файловых операций)
- ✅ Меньше потребление ресурсов
- ✅ Лучшая интеграция с Linux-инструментами
- ✅ Нативная поддержка Linux команд
- ✅ Более стабильная работа

**Недостатки:**
- ⚠️ Требует установки WSL2
- ⚠️ Нужно обновление Windows 10/11

### Без WSL2 (Hyper-V)

**Преимущества:**
- ✅ Работает на старых версиях Windows
- ✅ Не требует WSL2

**Недостатки:**
- ❌ Медленнее работа с файлами
- ❌ Больше потребление памяти
- ❌ Может конфликтовать с другими виртуальными машинами
- ❌ Менее стабильно

## 🔍 Как проверить текущий режим?

### 1. Проверка в Docker Desktop

1. Откройте Docker Desktop
2. Settings (⚙️) → General
3. Посмотрите на "Use WSL 2 based engine"
   - ✅ Включено = использует WSL2
   - ❌ Выключено = использует Hyper-V

### 2. Проверка через командную строку

```bash
# Проверить версию Docker
docker --version

# Проверить информацию о системе
docker info

# В выводе ищите:
# - "Operating System: Docker Desktop" (Hyper-V)
# - "Operating System: linux" (WSL2)
```

## 🚀 Установка WSL2 (если нужно)

### Шаг 1: Проверка текущего состояния

```powershell
# Проверить, установлен ли WSL
wsl --status

# Список установленных дистрибутивов
wsl --list --verbose
```

### Шаг 2: Установка WSL2

```powershell
# Включить WSL (требует перезагрузки)
wsl --install

# Или вручную:
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart

# Перезагрузите компьютер
```

### Шаг 3: Установка дистрибутива Linux

```powershell
# Установить Ubuntu (рекомендуется)
wsl --install -d Ubuntu

# Или через Microsoft Store:
# Найдите "Ubuntu" и установите
```

### Шаг 4: Настройка Docker Desktop

1. Откройте Docker Desktop
2. Settings → General
3. Включите "Use WSL 2 based engine"
4. Settings → Resources → WSL Integration
5. Включите интеграцию с вашим дистрибутивом (Ubuntu)
6. Нажмите "Apply & Restart"

## ⚙️ Настройка для проекта QuantTxt

### С WSL2 (рекомендуется)

```bash
# Работает быстрее
docker-compose up -d

# Файлы монтируются быстрее
# Лучшая производительность
```

### Без WSL2 (Hyper-V)

```bash
# Тоже работает, но медленнее
docker-compose up -d

# Может быть медленнее при работе с файлами
# Больше потребление памяти
```

## 🎯 Рекомендации

### Для разработки (рекомендуется WSL2)

Если вы:
- Разрабатываете на Windows
- Используете Linux-инструменты
- Нужна максимальная производительность

→ **Установите WSL2**

### Для простого использования (можно без WSL2)

Если вы:
- Просто запускаете Docker контейнеры
- Не нужна максимальная производительность
- Не хотите устанавливать WSL2

→ **Можно работать без WSL2**

## 🔧 Решение проблем

### Проблема: Docker Desktop не запускается

**С WSL2:**
```powershell
# Проверьте WSL
wsl --status

# Перезапустите WSL
wsl --shutdown
wsl --list --verbose
```

**Без WSL2:**
```powershell
# Проверьте Hyper-V
Get-WindowsOptionalFeature -Online -FeatureName Microsoft-Hyper-V

# Включите Hyper-V (требует перезагрузки)
Enable-WindowsOptionalFeature -Online -FeatureName Microsoft-Hyper-V -All
```

### Проблема: Медленная работа

**Решение:** Переключитесь на WSL2
1. Установите WSL2 (см. выше)
2. В Docker Desktop включите "Use WSL 2 based engine"
3. Перезапустите Docker Desktop

### Проблема: Конфликт с виртуальными машинами

**С WSL2:** Меньше конфликтов
**С Hyper-V:** Может конфликтовать с VirtualBox, VMware

## 📝 Итог

| Режим | Нужен ли? | Производительность | Стабильность |
|-------|-----------|-------------------|--------------|
| **WSL2** | Рекомендуется | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Hyper-V** | Не обязателен | ⭐⭐⭐ | ⭐⭐⭐ |

**Вывод:** WSL2 не обязателен, но **настоятельно рекомендуется** для лучшей производительности и стабильности.

## 🚀 Быстрая проверка

```bash
# Проверьте, какой режим используется
docker info | findstr "Operating System"

# Если видите "linux" - используется WSL2 ✅
# Если видите "Docker Desktop" - используется Hyper-V ⚠️
```


