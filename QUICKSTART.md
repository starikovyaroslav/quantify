# Быстрый старт QuantTxt

## 🚀 Запуск проекта одной командой

> 💡 **Примечание:** Docker Desktop может работать с WSL2 (рекомендуется) или без него (Hyper-V).
> Подробнее: см. [DOCKER_WSL_GUIDE.md](DOCKER_WSL_GUIDE.md)

### ⚡ Быстрый старт (все сервисы сразу)

```bash
# Перейти в директорию проекта
cd quanttxt

# ОДНА КОМАНДА для запуска всего:
docker-compose up -d
# Что это значит?
# - docker-compose: программа для управления несколькими контейнерами
# - up: запустить все сервисы
# - -d: фоновый режим (не блокирует терминал)
# Подробнее: см. DOCKER_EXPLAINED.md

# Это запустит:
# ✅ Backend API (FastAPI) - http://localhost:8000
# ✅ Frontend (Next.js) - http://localhost:3000
# ✅ Redis (очереди задач)
# ✅ PostgreSQL (база данных)
# ✅ Nginx (reverse proxy)
# ✅ Prometheus (метрики) - http://localhost:9090
# ✅ Grafana (мониторинг) - http://localhost:3001
```

#### Проверка статуса

```bash
# Проверить статус всех сервисов
docker-compose ps

# Просмотр логов
docker-compose logs -f api        # Backend
docker-compose logs -f frontend   # Frontend
docker-compose logs -f worker     # Celery worker
```

#### Проверка работы

```bash
# Health check Backend
curl http://localhost:8000/health

# Открыть в браузере:
# 🎨 Frontend: http://localhost:3000
# 📡 Backend API: http://localhost:8000/docs
# 📊 Grafana (мониторинг): http://localhost:3001 (admin/admin)
# 📈 Prometheus: http://localhost:9090
```

**Готово!** Проект запущен и готов к использованию.

### Вариант 2: Локальная разработка

#### Backend

```bash
# 1. Установка зависимостей
pip install -r requirements.txt

# 2. Запуск Redis (в отдельном терминале)
docker run -d -p 6379:6379 redis:7-alpine

# 3. Запуск FastAPI (в отдельном терминале)
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 4. Запуск Celery worker (в отдельном терминале)
celery -A app.celery_app worker --loglevel=info
```

#### Frontend

```bash
# 1. Перейти в директорию frontend
cd frontend

# 2. Установка зависимостей
npm install

# 3. Запуск dev сервера
npm run dev
```

**Доступ:**

- Frontend: http://localhost:3000
- Backend API: http://localhost:8000/docs

## Использование API

### Через веб-интерфейс

1. Откройте http://localhost:8000/docs
2. Найдите endpoint `POST /api/v1/quantize/`
3. Нажмите "Try it out"
4. Загрузите изображение
5. Установите параметры (width, height, quality)
6. Нажмите "Execute"
7. Скопируйте `task_id` из ответа
8. Используйте `GET /api/v1/quantize/status/{task_id}` для проверки статуса
9. Используйте `GET /api/v1/quantize/result/{task_id}` для получения результата

### Через Python скрипт

```bash
# Установить зависимости
pip install requests

# Запустить пример
python example_usage.py path/to/image.jpg 200 200 5
```

### Через curl

```bash
# 1. Загрузить изображение
curl -X POST "http://localhost:8000/api/v1/quantize/" \
  -F "file=@image.jpg" \
  -F "width=200" \
  -F "height=200" \
  -F "quality=5"

# Ответ:
# {"task_id": "uuid-here", "status": "processing", "estimated_time": 2}

# 2. Проверить статус
curl "http://localhost:8000/api/v1/quantize/status/{task_id}"

# 3. Получить результат
curl "http://localhost:8000/api/v1/quantize/result/{task_id}" -o result.txt
```

## Просмотр результата

### Windows (Блокнот)

1. Откройте файл `.txt` в Блокноте
2. Выберите шрифт: `Tahoma`
3. Установите размер: `1`
4. Отключите перенос слов: `Формат → Перенос по словам` (снять галочку)
5. Уменьшите масштаб для лучшего обзора

### Mac (TextEdit)

1. Откройте файл `.txt` в TextEdit
2. `Формат → Шрифт → Показать шрифты`
3. Выберите `Tahoma`, размер `1`
4. `Формат → Перенос текста → Отключить`

### Linux

```bash
# Использовать gedit или другой редактор
gedit result.txt

# Или через cat (для просмотра структуры)
cat result.txt
```

## 🧪 Тестирование

### Тест алгоритма квантования

```bash
# Запустить тестовый скрипт
python test_quantizer.py

# Результат будет сохранен в test_output.txt
# Откройте файл в текстовом редакторе (Tahoma, 1pt) для просмотра
```

### Проверка качества кода

```bash
# Backend проверки
make format          # Форматирование Python
make lint            # Линтинг
make type-check      # Проверка типов
make check           # Все проверки

# Frontend проверки
cd frontend
npm run format       # Форматирование
npm run lint:check   # Линтинг
npm run type-check   # Проверка типов
npm run check        # Все проверки
```

## 📊 Мониторинг нагрузки

### Доступ к мониторингу

После запуска `docker-compose up -d` доступны:

1. **Grafana Dashboard** - http://localhost:3001

   - Логин: `admin`
   - Пароль: `admin` (измените в продакшене!)
   - Метрики: запросы, время ответа, использование ресурсов

2. **Prometheus** - http://localhost:9090
   - Сырые метрики
   - Запросы: `quanttxt_requests_total`
   - Время ответа: `quanttxt_request_duration_seconds`

### Просмотр метрик через командную строку

```bash
# Использование ресурсов контейнеров
docker stats

# Логи с метриками
docker-compose logs -f api | grep -i metric

# Количество активных задач в Redis
docker-compose exec redis redis-cli LLEN celery

# Статус очереди Celery
docker-compose exec worker celery -A app.celery_app inspect active
```

### Основные метрики

- **Запросы в секунду**: `rate(quanttxt_requests_total[1m])`
- **Время обработки**: `quanttxt_request_duration_seconds`
- **Ошибки**: `quanttxt_requests_total{status=~"5.."}`
- **Размер очереди**: через Redis/Celery
- **Использование CPU/RAM**: через `docker stats`

### Примеры запросов в Prometheus

```promql
# Запросы в секунду
rate(quanttxt_requests_total[1m])

# Среднее время ответа
rate(quanttxt_request_duration_seconds_sum[5m]) / rate(quanttxt_request_duration_seconds_count[5m])

# Количество ошибок
sum(rate(quanttxt_requests_total{status=~"5.."}[1m]))
```

## ⚠️ Решение проблем

### Ошибка "500 Internal Server Error"

Если видите ошибку:

```
unable to get image 'quanttxt-api':
request returned 500 Internal Server Error
```

**Решение:**

1. Убедитесь, что Docker Desktop запущен (иконка в трее должна быть зеленая)
2. Перезапустите Docker Desktop
3. Попробуйте собрать образы вручную:
   ```bash
   docker-compose build
   docker-compose up -d
   ```

Подробнее см. [TROUBLESHOOTING.md](TROUBLESHOOTING.md)

## 🛑 Остановка сервисов

```bash
# Остановить все сервисы (сохраняет данные)
docker-compose down

# Остановить и удалить volumes (удалит ВСЕ данные!)
docker-compose down -v

# Остановить только определенный сервис
docker-compose stop api
docker-compose stop frontend

# Перезапустить сервис
docker-compose restart api
```

## Troubleshooting

### Порт уже занят

```bash
# Изменить порты в docker-compose.yml
ports:
  - "8001:8000"  # Вместо 8000:8000
```

### Redis не подключается

```bash
# Проверить логи Redis
docker-compose logs redis

# Перезапустить Redis
docker-compose restart redis
```

### Worker не обрабатывает задачи

```bash
# Проверить логи worker
docker-compose logs worker

# Перезапустить worker
docker-compose restart worker
```

### Ошибка "Task not found"

- Задачи хранятся в Redis 1 час
- Проверьте, что Redis работает: `docker-compose ps redis`
- Проверьте логи: `docker-compose logs api`

## Производительность

### Рекомендуемые параметры

- **Маленькие изображения (до 500x500):**

  - Width: 100-200
  - Height: 100-200
  - Quality: 5-7

- **Средние изображения (500x500 - 2000x2000):**

  - Width: 200-400
  - Height: 200-400
  - Quality: 5-8

- **Большие изображения (2000x2000+):**
  - Width: 400-800
  - Height: 400-800
  - Quality: 6-10

### Оптимизация

- Уменьшите `width` и `height` для быстрой обработки
- Уменьшите `quality` для меньшего размера файла
- Увеличьте количество workers в `docker-compose.yml`

## Следующие шаги

1. Прочитайте [README.md](README.md) для полной документации
2. Изучите [ARCHITECTURE.md](ARCHITECTURE.md) для понимания архитектуры
3. См. [DEPLOYMENT.md](DEPLOYMENT.md) для продакшен развертывания
