# QuantTxt - Оптическое квантование изображений в текст

> 📖 **Инструкции по запуску:** см. [QUICKSTART.md](QUICKSTART.md)
> 🔥 **Режим разработки с hot reload:** см. [DEV_GUIDE.md](DEV_GUIDE.md)
> 🚀 **Быстрый старт:** `docker-compose up -d` → http://localhost:3000

## 🎯 Описание

QuantTxt - это высокопроизводительная система для преобразования изображений в текстовые файлы UTF-16 LE, где каждый Unicode-символ представляет цветной пиксель. При минимальном размере шрифта (Tahoma 1pt) в обычном текстовом редакторе возникает полноценное цветное изображение.

## 🏗️ Архитектура

### Технологический стек

**Backend:**

- Python 3.11+ с FastAPI для высокопроизводительного API
- Celery + Redis для асинхронной обработки задач
- Pillow (PIL) для обработки изображений
- NumPy для математических операций

**Инфраструктура:**

- Redis - очереди задач и кэширование
- PostgreSQL - метаданные и статистика
- Nginx - reverse proxy, rate limiting, статика
- Docker + Docker Compose - контейнеризация
- Prometheus + Grafana - мониторинг

**Безопасность:**

- Rate limiting на уровне Nginx и приложения
- Валидация входных данных
- Защита от перегрузки (circuit breaker)
- DDoS защита через Nginx

### Архитектурная схема

```
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
       ▼
┌─────────────────┐     ┌──────────────┐
│   Nginx         │────▶│  Rate Limit  │
│   (Reverse      │     │  (Redis)     │
│    Proxy)       │     └──────────────┘
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   FastAPI       │
│   (API Gateway) │
└────────┬────────┘
         │
         ├──────────────┐
         │              │
         ▼              ▼
┌──────────────┐  ┌──────────────┐
│   Celery     │  │   Redis      │
│   Workers    │  │   (Queue)    │
└──────┬───────┘  └──────────────┘
       │
       ▼
┌──────────────┐
│  Quantizer   │
│  Algorithm   │
└──────────────┘
```

## 🚀 Быстрый старт (одна команда)

```bash
# Запустить ВСЕ сервисы одной командой:
docker-compose up -d
```

**Доступ:**

- 🎨 **Frontend**: http://localhost:3000
- 📡 **Backend API**: http://localhost:8000/docs
- 📊 **Grafana (мониторинг)**: http://localhost:3001 (admin/admin)
- 📈 **Prometheus**: http://localhost:9090

**Проверка:**

```bash
docker-compose ps          # Статус всех сервисов
docker stats               # Использование ресурсов
docker-compose logs -f api # Логи Backend
```

**Остановка:**

```bash
docker-compose down        # Остановить все
docker-compose down -v     # Остановить и удалить данные
```

### Вариант 2: Локальная разработка

#### Backend

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск Redis (в отдельном терминале)
docker run -d -p 6379:6379 redis:7-alpine

# Запуск FastAPI
uvicorn app.main:app --reload

# Запуск Celery worker (в отдельном терминале)
celery -A app.celery_app worker --loglevel=info
```

#### Frontend

```bash
cd frontend

# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

**Доступ:**

- Frontend: http://localhost:3000
- Backend API: http://localhost:8000

### Проверка работы

```bash
# Health check
curl http://localhost:8000/health

# Или откройте в браузере
# http://localhost:8000/docs - API документация
# http://localhost:3000 - Frontend интерфейс
```

📖 **Подробные инструкции:** см. [QUICKSTART.md](QUICKSTART.md)

## 📡 API Endpoints

### POST /api/v1/quantize

Преобразует изображение в текст

**Request:**

- `file`: изображение (multipart/form-data)
- `width`: ширина результата (опционально, по умолчанию 200)
- `height`: высота результата (опционально, по умолчанию 200)
- `quality`: качество (1-10, по умолчанию 5)

**Response:**

```json
{
  "task_id": "uuid",
  "status": "processing",
  "estimated_time": 5
}
```

### GET /api/v1/status/{task_id}

Проверка статуса задачи

### GET /api/v1/result/{task_id}

Получение результата (текстовый файл)

## 🔬 Алгоритм

1. **Предобработка изображения:**

   - Ресайз до целевого размера
   - Оптимизация контраста

2. **Цветовое квантование:**

   - Упрощение палитры до 256 цветов
   - Сохранение основных цветовых переходов

3. **Оптическое квантование:**

   - Для каждого пикселя вычисляется визуальный вес
   - Подбор Unicode-символа с ближайшим оптическим весом
   - Учет плотности штрихов, формы символа

4. **Генерация текста:**
   - Создание UTF-16 LE файла
   - Оптимизация размера

## 📊 Производительность

- Обработка изображения 200x200: ~0.5-2 секунды
- Пропускная способность: до 1000 запросов/сек (с горизонтальным масштабированием)
- Поддержка до 10,000 одновременных задач в очереди

## 🔒 Безопасность

- Rate limiting: 100 запросов/минуту на IP
- Максимальный размер файла: 10MB
- Валидация форматов: только изображения
- Circuit breaker для защиты от перегрузки

## 🔧 Code Quality

Проект использует enterprise-стандарты качества кода:

- **Python:** Black, isort, flake8, mypy, pylint
- **TypeScript:** ESLint, Prettier, TypeScript strict mode
- **Pre-commit hooks** для автоматических проверок

```bash
# Быстрая настройка
make install-dev
pre-commit install

# Проверка кода
make check              # Backend
make frontend-check     # Frontend
make all-check          # Все
```

См. [CODE_QUALITY.md](CODE_QUALITY.md) для деталей.

## 🔌 Расширения Cursor/VS Code

Рекомендуемые расширения для разработки см. в [CURSOR_EXTENSIONS.md](CURSOR_EXTENSIONS.md)

## 📝 Лицензия

MIT
