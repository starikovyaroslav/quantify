# QuantTxt - Полный обзор проекта

## 🎯 О проекте

QuantTxt - это полнофункциональная система для преобразования изображений в текст с помощью оптического квантования Unicode-символов. Проект состоит из высокопроизводительного бэкенда на FastAPI и современного фронтенда на Next.js.

## 📁 Структура проекта

```
quanttxt/
├── app/                    # FastAPI бэкенд
│   ├── api/v1/            # API endpoints
│   ├── core/               # Конфигурация, Redis
│   ├── quantizer/          # Алгоритм квантования
│   ├── tasks/              # Celery задачи
│   └── main.py             # Точка входа
├── frontend/               # Next.js фронтенд
│   ├── app/                # Next.js App Router
│   ├── components/         # React компоненты
│   └── lib/                # Утилиты и API клиент
├── nginx/                  # Nginx конфигурация
├── docker-compose.yml      # Docker Compose
└── README.md               # Основная документация
```

## 🚀 Быстрый старт

### 1. Бэкенд

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск через Docker Compose
docker-compose up -d

# Или локально
uvicorn app.main:app --reload
celery -A app.celery_app worker --loglevel=info
```

### 2. Фронтенд

```bash
cd frontend
npm install
npm run dev
```

## 🏗️ Архитектура

### Backend Stack
- **FastAPI** - Асинхронный веб-фреймворк
- **Celery + Redis** - Очереди задач
- **PostgreSQL** - База данных
- **Pillow + NumPy** - Обработка изображений
- **Nginx** - Reverse proxy, rate limiting

### Frontend Stack
- **Next.js 14** - React фреймворк
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Framer Motion** - Анимации
- **React Dropzone** - Drag & Drop

## ✨ Ключевые особенности

### Алгоритм квантования
- ✅ Продвинутое цветовое квантование (Median Cut)
- ✅ Цвето-осознанный подбор Unicode-символов
- ✅ Предобработка изображений
- ✅ Адаптивное качество (1-10)

### Дизайн фронтенда
- ✅ Glassmorphism 2.0
- ✅ 3D Spatial Background
- ✅ AI Micro-UI анимации
- ✅ Полная адаптивность
- ✅ Темная тема

### Инфраструктура
- ✅ Горизонтальное масштабирование
- ✅ Защита от DDoS
- ✅ Rate limiting
- ✅ Мониторинг готов
- ✅ Docker контейнеризация

## 📡 API Endpoints

### POST /api/v1/quantize/
Загрузка и квантование изображения

**Request:**
- `file`: изображение (multipart/form-data)
- `width`: ширина (50-1000)
- `height`: высота (50-1000)
- `quality`: качество (1-10)

**Response:**
```json
{
  "task_id": "uuid",
  "status": "processing",
  "estimated_time": 5
}
```

### GET /api/v1/quantize/status/{task_id}
Проверка статуса задачи

### GET /api/v1/quantize/result/{task_id}
Получение результата (текстовый файл)

## 🎨 Страницы фронтенда

1. **Главная (/**) - Editor**
   - Drag & Drop загрузка
   - Live Preview
   - Controls Panel
   - Квантование в реальном времени

2. **Галерея (/gallery)**
   - Примеры готовых QuantTxt
   - Grid layout
   - Анимации появления

3. **Тарифы (/pricing)**
   - Free/Pro/Enterprise планы
   - Glassmorphism карточки
   - Градиентные кнопки

4. **Профиль (/profile)**
   - API ключи
   - История операций
   - Настройки

## 🔧 Конфигурация

### Backend (.env)
```env
REDIS_HOST=localhost
REDIS_PORT=6379
DATABASE_URL=postgresql+asyncpg://...
CELERY_BROKER_URL=redis://localhost:6379/0
RATE_LIMIT_PER_MINUTE=100
```

### Frontend (.env.local)
```env
NEXT_PUBLIC_API_URL=http://localhost:8000
```

## 📊 Производительность

- **Обработка 200x200:** 0.5-2 секунды
- **Пропускная способность:** до 1000 req/s
- **Очередь задач:** до 10,000 задач
- **Масштабирование:** горизонтальное (API + Workers)

## 🛡️ Безопасность

- Rate limiting (100 req/min, 1000 req/hour)
- Валидация входных данных
- Ограничение размера файлов (10MB)
- DDoS защита через Nginx
- Изоляция через Docker

## 📚 Документация

- **README.md** - Основная документация
- **ARCHITECTURE.md** - Детальная архитектура
- **DEPLOYMENT.md** - Руководство по развертыванию
- **COMPETITIVE_ANALYSIS.md** - Сравнение с конкурентами
- **IMPROVEMENTS.md** - Описание улучшений
- **FRONTEND_SETUP.md** - Настройка фронтенда
- **QUICKSTART.md** - Быстрый старт

## 🚢 Деплой

### Docker Compose (разработка)
```bash
docker-compose up -d
```

### Kubernetes (продакшен)
См. `DEPLOYMENT.md` для детальных инструкций.

### Vercel (фронтенд)
```bash
cd frontend
vercel
```

## 🧪 Тестирование

```bash
# Тест алгоритма
python test_quantizer.py

# Тест API
python example_usage.py image.jpg
```

## 🔄 Workflow

1. Пользователь загружает изображение через фронтенд
2. Фронтенд отправляет запрос на `/api/v1/quantize/`
3. Бэкенд создает задачу в Celery
4. Celery Worker обрабатывает изображение
5. Результат сохраняется и доступен через API
6. Фронтенд polling статуса и отображает результат

## 📈 Roadmap

### Завершено ✅
- Алгоритм квантования
- FastAPI бэкенд
- Celery интеграция
- Next.js фронтенд
- Glassmorphism дизайн
- Docker контейнеризация

### В планах 🚀
- Аутентификация пользователей
- Реальная галерея с БД
- Batch обработка
- Экспорт в разные форматы
- WebSocket для real-time обновлений
- Мобильное приложение

## 🤝 Контрибуция

Проект открыт для улучшений! См. документацию для деталей.

## 📄 Лицензия

MIT

## 👥 Авторы

Разработано с использованием современных технологий и best practices.

---

**QuantTxt** - Преобразуйте изображения в текст с помощью оптического квантования Unicode-символов.




